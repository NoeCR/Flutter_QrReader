import 'package:flutter/material.dart';

class DirectionsPage extends StatelessWidget {
  static final pageName = 'directions';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Direction Page'),
    );
  }
}
